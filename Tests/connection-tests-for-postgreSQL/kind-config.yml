# three node (one workers) cluster config
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
- role: worker
  extraMounts:
  - hostPath: /mnt/k8s-data    # The path on your EC2
    containerPath: /var/local-path-provisioner # The path inside the Kind node