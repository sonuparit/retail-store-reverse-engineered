{{- if .Values.secret.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secret.name }}-secret
data:
  RETAIL_CATALOG_PERSISTENCE_USER: {{ .Values.secret.username | b64enc | quote }}
  RETAIL_CATALOG_PERSISTENCE_PASSWORD: "{{ include "catalog.password" . }}"
{{- end }}