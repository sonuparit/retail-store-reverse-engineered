# service.yaml
apiVersion: v1
kind: Service

metadata:
  name: postgres-exporter
  namespace: dev
  labels:
    app: postgres-exporter

spec:
  type: ClusterIP

  selector:
    app: postgres-exporter

  ports:
  - name: metrics
    port: 9187
    targetPort: metrics